<div class="container">
  <div class="col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">

<h1>contact</h1>

<% @contacts.each do |contact| %>
  <p>name:<%= contact.name %></p>
  <p>email:<%= contact.email %></p>
  <p>content:<%= contact.content %></p>
  
<% end %>

<%= link_to "お問い合わせ料金4500円/回", new_contact_path %>

</div>
</div>